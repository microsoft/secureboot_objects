name: PR Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build Binaries
    uses: ./.github/workflows/BuildBinaries.yml
    with:
      python-version: '3.11'
